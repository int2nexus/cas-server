nexus-client 가 배포되었습니다.

서비스: {{ include "nexus-client.fullname" . }} (port {{ .Values.service.port }} → {{ .Values.service.targetPort }})
이미지: {{ .Values.image.repository }}:{{ .Values.image.tag }}

Caddy 가 정적 SPA 서빙 + /api/* → {{ .Values.backendUrl }} 프록시.
브라우저는 이 FE 오리진과만 통신하므로 CORS 가 발생하지 않습니다.

{{- if .Values.ingress.enabled }}

접속: http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.host }}/
{{- else }}

Ingress 비활성. 로컬 확인:
  kubectl port-forward svc/{{ include "nexus-client.fullname" . }} 8080:{{ .Values.service.port }}
  → http://localhost:8080/
{{- end }}

전제:
  - 백엔드(nexus-server)가 클러스터 내부에서 {{ .Values.backendUrl }} 로 접근 가능해야 합니다.
  - 이미지: 백엔드(nexus-server)가 브라우저에서 닿는 CAS URL 을 반환해야 합니다
    (cas-server ingress 를 켜고 그 주소를 반환하도록). FE 는 받은 URL 을 그대로 <img> 로 로드합니다.

상태 확인:
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }}
